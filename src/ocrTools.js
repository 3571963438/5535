export const ocrTools=[{name:'ocr_image',description:'从图片中识别文字（支持多种语言）',inputSchema:{type:'object',properties:{image_path:{type:'string',description:'图片文件路径（支持 jpg, png, bmp, tiff 等格式）'},language:{type:'string',description:'识别语言代码，默认为 eng（英文）。支持：eng, chi_sim（简体中文）, chi_tra（繁体中文）, jpn（日语）, kor（韩语）等',default:'eng'},psm:{type:'number',description:'页面分割模式 (0-13)，默认为 3（自动）',default:3},output_format:{type:'string',description:'输出格式：text（纯文本）, json（详细信息包含置信度）',enum:['text','json'],default:'text'}},required:['image_path']}},{name:'ocr_image_base64',description:'从 Base64 编码的图片中识别文字',inputSchema:{type:'object',properties:{image_base64:{type:'string',description:'Base64 编码的图片数据'},language:{type:'string',description:'识别语言代码，默认为 eng',default:'eng'},psm:{type:'number',description:'页面分割模式',default:3},output_format:{type:'string',enum:['text','json'],default:'text'}},required:['image_base64']}},{name:'ocr_batch',description:'批量识别多张图片',inputSchema:{type:'object',properties:{image_paths:{type:'array',items:{type:'string'},description:'图片路径数组'},language:{type:'string',default:'eng'},output_format:{type:'string',enum:['text','json'],default:'text'}},required:['image_paths']}},{name:'ocr_with_preprocessing',description:'预处理图片后进行 OCR（提高识别准确度）',inputSchema:{type:'object',properties:{image_path:{type:'string',description:'图片路径'},language:{type:'string',default:'eng'},preprocessing:{type:'object',properties:{grayscale:{type:'boolean',description:'转为灰度图',default:true},threshold:{type:'boolean',description:'二值化处理',default:true},denoise:{type:'boolean',description:'降噪处理',default:false},resize:{type:'number',description:'缩放比例（如 2 表示放大2倍）'}},description:'预处理选项'}},required:['image_path']}},{name:'get_supported_languages',description:'获取所有支持的 OCR 语言列表',inputSchema:{type:'object',properties:{},required:[]}}];
